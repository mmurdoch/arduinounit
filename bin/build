#!/bin/bash -x

# figure out how to run scons...
if type -P -t scons.py > /dev/null
then
  scons=`type -P scons.py`
else
  scons=`type -P scons`
fi

# run scons in firmware directory
cd `dirname $0`
cd ../firmware
exec "$scons" "$@"
