#!/bin/bash

if [ "$(type -t arduino_dir)" != "function" ]
then
    . "$(cd "$(dirname "${BASH_SOURCE[0]}")"; pwd)"/arduino_dir
fi

avr_root() {
    echo "$(arduino_dir "$@")/hardware/tools/avr"
}

if [[ "${BASH_SOURCE[0]}" == "${0}" ]]
then
    avr_root "$@"
fi
